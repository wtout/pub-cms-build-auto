---
# Ensure python required packages are installed on localhost (always run)
- name: Install Python packages
  hosts: localhost
  any_errors_fatal: true
  gather_facts: no

  roles:
    - role: pypkgs
