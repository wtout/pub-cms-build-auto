---
# Define customer build inventory (always run)
- name: Define customer build inventory
  hosts: localhost
  any_errors_fatal: true
  gather_facts: no

  roles:
    - role: "{{ auto_dir }}/roles/define_inventory"
