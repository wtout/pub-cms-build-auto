---
# Variables for splunk deployers
vm:
  cpu: "{{ vm_vars.splunk.spldpl.cpu }}"
  memory: "{{ vm_vars.splunk.spldpl.memory }}"
  disk0: "{{ vm_vars.splunk.spldpl.disk0 }}"
  disk1: "{{ vm_vars.splunk.spldpl.disk1 | default(0) }}"
  disk2: "{{ vm_vars.splunk.spldpl.disk2 | default(0) }}"
  disk3: "{{ vm_vars.splunk.spldpl.disk3 | default(0) }}"
  name: "{{ customer.name_prefix }}{{ inventory_hostname[:-2] }}{{ datacenters[datacenter.name]['deptype'] }}{{ inventory_hostname[-2:] }}"

netconfig:
  netname1: "{{ vm_vars.splunk.spldpl.nic1.name }}"
  ippool1: "{{ vm_vars.splunk.spldpl.nic1.ip_pool | default([]) }}"
  octetpool1: "{{ vm_vars.splunk.spldpl.nic1.ip_octetpool | default([]) }}"
  gateway1: "{{ customer.octets + '.' + vm_vars.splunk.spldpl.nic1.gtw_octet if vm_vars.splunk.spldpl.nic1.gtw_octet != '' else '' }}"
  netmask1: "{{ vm_vars.splunk.spldpl.nic1.netmask }}"
  network1: "{{ vm_vars.splunk.spldpl.nic1.network }}"

  netname2: "{{ vm_vars.splunk.spldpl.nic2.name }}"
  ippool2: "{{ vm_vars.splunk.spldpl.nic2.ip_pool | default([]) }}"
  octetpool2: "{{ vm_vars.splunk.spldpl.nic2.ip_octetpool | default([]) }}"
  gateway2: "{{ customer.octets + '.' + vm_vars.splunk.spldpl.nic2.gtw_octet if vm_vars.splunk.spldpl.nic2.gtw_octet != '' else '' }}"
  netmask2: "{{ vm_vars.splunk.spldpl.nic2.netmask }}"
  network2: "{{ vm_vars.splunk.spldpl.nic2.network }}"
