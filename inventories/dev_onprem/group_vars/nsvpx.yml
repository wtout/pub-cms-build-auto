---
# Variables for netscaler hosts
credentials:
  username: "nsroot"
  password: "C1sc0123"

vm:
  cpu: "{{vm_vars.nspvx.cpu}}"
  memory: "{{vm_vars.nspvx.memory}}"
  disk0: "{{vm_vars.nspvx.disk0}}"
  disk1: "{{vm_vars.nspvx.disk1 | default(0)}}"
  disk2: "{{vm_vars.nspvx.disk2 | default(0)}}"
  disk3: "{{vm_vars.nspvx.disk3 | default(0)}}"
  name: "{{customer.name_prefix}}{{inventory_hostname[:-2]}}{{datacenters[datacenter.name]['deptype']}}{{inventory_hostname[-2:]}}"

netconfig:
  netname1: "{{vm_vars.nspvx.nic1.name}}"
  ippool1: "{{vm_vars.nspvx.nic1.ip_pool | default([])}}"
  octetpool1: "{{vm_vars.nspvx.nic1.ip_octetpool | default([])}}"
  gateway1: "{{vm_vars.nspvx.nic1.gtw_octet}}"
  netmask1: "{{vm_vars.nspvx.nic1.netmask}}"
  network1: "{{vm_vars.nspvx.nic1.network}}"

  netname2: "{{vm_vars.nspvx.nic2.name}}"
  ippool2: "{{vm_vars.nspvx.nic2.ip_pool | default([])}}"
  octetpool2: "{{vm_vars.nspvx.nic2.ip_octetpool | default([])}}"
  gateway2: "{{vm_vars.nspvx.nic2.gtw_octet}}"
  netmask2: "{{vm_vars.nspvx.nic2.netmask}}"
  network2: "{{vm_vars.nspvx.nic2.network}}"
