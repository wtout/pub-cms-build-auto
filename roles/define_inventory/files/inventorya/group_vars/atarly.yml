---
# Variables for ata relay hosts
credentials:
  username: "{{ NONEM7_USER|default('') }}"
  password: "{{ NONEM7_PASS|default('') }}"

user:
  name: "{{ vm_vars.atarly.user.name if vm_vars.atarly.user.name is defined else '' }}"
  password: "{{ vm_vars.atarly.user.password if vm_vars.atarly.user.password is defined else '' }}"

vm:
  cpu: "{{ vm_vars.atarly.cpu }}"
  memory: "{{ vm_vars.atarly.memory }}"
  disk0: "{{ vm_vars.atarly.disk0 }}"
  disk1: "{{ vm_vars.atarly.disk1 | default(0) }}"
  disk2: "{{ vm_vars.atarly.disk2 | default(0) }}"
  disk3: "{{ vm_vars.atarly.disk3 | default(0) }}"
  disk_provisioning: "{{ vm_vars.atarly.disk_provisioning if vm_vars.atarly.disk_provisioning != '' else '' }}"
  hostmap: "{{ vm_vars.atarly.hostmap | default([]) }}"
  name: "{{ vm_vars.atarly.vmname if vm_vars.atarly.vmname != '' else (customer.primary.name_prefix + inventory_hostname[:-2] + datacenters[datacenter.primary.name]['deptype'] + inventory_hostname[-2:]) }}"

netconfig:
  intname1: "{{ vm_vars.atarly.nic1.intname }}"
  ippool1: "{{ vm_vars.atarly.nic1.ip_pool | default([]) }}"
  octetpool1: "{{ vm_vars.atarly.nic1.ip_octetpool | default([]) }}"
  gateway1: "{{ vm_vars.atarly.nic1.gtw if vm_vars.atarly.nic1.gtw != '' else (customer.primary.octets + '.' + vm_vars.atarly.nic1.gtw_octet) if vm_vars.atarly.nic1.gtw_octet != '' else '' }}"
  netmask1: "{{ vm_vars.atarly.nic1.netmask }}"
  network1: "{{ vm_vars.atarly.nic1.netname if vm_vars.atarly.nic1.netname != '' else (customer.primary.name_prefix + '-' + vm_vars.atarly.nic1.netbase) if vm_vars.atarly.nic1.netbase != '' else '' }}"
  ipv6_pool1: "{{ vm_vars.atarly.nic1.ipv6_pool | default([]) }}"
  ipv6_gtw1: "{{ vm_vars.atarly.nic1.ipv6_gtw | default('') }}"

  intname2: "{{ vm_vars.atarly.nic2.intname }}"
  ippool2: "{{ vm_vars.atarly.nic2.ip_pool | default([]) }}"
  octetpool2: "{{ vm_vars.atarly.nic2.ip_octetpool | default([]) }}"
  gateway2: "{{ vm_vars.atarly.nic2.gtw if vm_vars.atarly.nic2.gtw != '' else (customer.primary.octets + '.' + vm_vars.atarly.nic2.gtw_octet) if vm_vars.atarly.nic2.gtw_octet != '' else '' }}"
  netmask2: "{{ vm_vars.atarly.nic2.netmask }}"
  network2: "{{ vm_vars.atarly.nic2.netname if vm_vars.atarly.nic2.netname != '' else (customer.primary.name_prefix + '-' + vm_vars.atarly.nic2.netbase) if vm_vars.atarly.nic2.netbase != '' else '' }}"
  ipv6_pool2: "{{ vm_vars.atarly.nic2.ipv6_pool | default([]) }}"
  ipv6_gtw2: "{{ vm_vars.atarly.nic2.ipv6_gtw | default('') }}"
