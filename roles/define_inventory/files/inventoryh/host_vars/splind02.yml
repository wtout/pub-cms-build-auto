ansible_host: "{{ netconfig.ippool1[inventory_hostname[-2:]|int - 1] if netconfig.ippool1 | length >= 2 else customer.primary.octets + '.' + netconfig.octetpool1[inventory_hostname[-2:]|int - 1]|string }}"
