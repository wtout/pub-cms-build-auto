---
# Variables for csr hosts
credentials:
  username: 'admin'
  password: "{{ CSR_PASS|default('') }}"

vm:
  cpu: "{{ vm_vars.csr.cpu }}"
  memory: "{{ vm_vars.csr.memory }}"
  disk0: "{{ vm_vars.csr.disk0 }}"
  disk1: "{{ vm_vars.csr.disk1 | default(0) }}"
  disk2: "{{ vm_vars.csr.disk2 | default(0) }}"
  disk3: "{{ vm_vars.csr.disk3 | default(0) }}"
  name: "{{ customer.secondary_name_prefix + inventory_hostname[2:-2] + datacenters[datacenter.name]['deptype'] + inventory_hostname[-2:] }}"

netconfig:
  netname1: "{{ vm_vars.csr.nic1.name }}"
  ippool1: "{{ vm_vars.csr.nic1.ip_pool | default([]) }}"
  octetpool1: "{{ vm_vars.csr.nic1.ip_octetpool | default([]) }}"
  gateway1: "{{ customer.secondary_octets + '.' + vm_vars.csr.nic1.gtw_octet if vm_vars.csr.nic1.gtw_octet != '' else '' }}"
  netmask1: "{{ vm_vars.csr.nic1.netmask }}"
  network1: "{{ customer.secondary_name_prefix + '-' + vm_vars.csr.nic1.network }}"

  netname2: "{{ vm_vars.csr.nic2.name }}"
  ippool2: "{{ vm_vars.csr.nic2.ip_pool | default([]) }}"
  octetpool2: "{{ vm_vars.csr.nic2.ip_octetpool | default([]) }}"
  gateway2: "{{ customer.secondary_octets + '.' + vm_vars.csr.nic2.gtw_octet if vm_vars.csr.nic2.gtw_octet != '' else '' }}"
  netmask2: "{{ vm_vars.csr.nic2.netmask }}"
  network2: "{{ customer.secondary_name_prefix + '-' + vm_vars.csr.nic2.network }}"

  netname3: "{{ vm_vars.csr.nic3.name }}"
  ippool3: "{{ vm_vars.csr.nic3.ip_pool | default([]) }}"
  octetpool3: "{{ vm_vars.csr.nic3.ip_octetpool | default([]) }}"
  gateway3: "{{ customer.secondary_octets + '.' + vm_vars.csr.nic3.gtw_octet if vm_vars.csr.nic3.gtw_octet != '' else '' }}"
  netmask3: "{{ vm_vars.csr.nic3.netmask }}"
  network3: "{{ customer.secondary_name_prefix + '-' + vm_vars.csr.nic3.network }}"
