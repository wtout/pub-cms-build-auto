---
# handlers file for certificates
- name: puppet sign certs
  shell: /usr/local/bin/puppet cert sign {{vm.name}}.{{sysconfig.domain_name}}
  delegate_to: "{{groups['puppet']|first}}"
  ignore_errors: true
  tags: certificates
