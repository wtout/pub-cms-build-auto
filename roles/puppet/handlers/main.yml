---
# handlers file for puppet
- name: puppet sign certs
  shell: >
    /usr/local/bin/puppet cert sign {{vm.name}}.{{sysconfig.domain_name}}
  become: true
  delegate_to: "{{groups['puppet']|first}}"
  ignore_errors: true
  tags: puppet
