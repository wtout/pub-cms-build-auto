---
# Tasks to install Python Package
- name: Install Package {{ pypkg }}
  pip:
    name: "{{ pypkg }}"
    state: present
    extra_args: --user
  environment:
    http_proxy: "{{ proxyaddress }}"
  tags: [ 'always', 'pypkgs' ]
