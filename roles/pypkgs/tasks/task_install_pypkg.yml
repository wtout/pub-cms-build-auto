---
# Tasks to install Python Package
- name: Install Package {{ pypkg }}
  pip:
    name: "{{ pypkg }}"
    state: "{{ 'present' if auto_dir is search('cdra') else 'latest' }}"
    extra_args: --user
  environment:
    http_proxy: "{{ proxyaddress }}"
  tags: [ 'always', 'pypkgs' ]
