---
# tasks to update /etc/hosts on VM
- name: Copy hosts file to VM
  template:
    src: "{{ role_path }}/templates/{{ 'dr' if 'dr' in group_names else '' }}hosts.tmpl"
    dest: '/etc/hosts'
  become: "{{ true if inventory_hostname is search('em7') else false }}"
  tags: vm_configuration
